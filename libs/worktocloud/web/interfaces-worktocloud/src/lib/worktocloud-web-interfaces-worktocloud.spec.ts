import { worktocloudWebInterfacesWorktocloud } from './worktocloud-web-interfaces-worktocloud';

describe('worktocloudWebInterfacesWorktocloud', () => {
  it('should work', () => {
    expect(worktocloudWebInterfacesWorktocloud()).toEqual(
      'worktocloud-web-interfaces-worktocloud'
    );
  });
});
